1.  A class is essentially a template which defines the form of an object;  an object is an instance of a given class.

2.  Using the 'class' keyword.

3.  Each object has its own copy of any instance variables.

4.  MyCounter counter;  counter = new MyCounter();

5.  double myMeth( int a, int b ) { /* code here */ }

6.  return myRetVal;

7.  Constructor method is named to match the class.

8.  new creates a new object and initialises it.

9.  Garbage collection is a memory-freeing process which clears unused objects.  finalize() is called on an object when it is garbage collected.

10. 'this' is a reference to the current object

11. Yes.

12. void.
